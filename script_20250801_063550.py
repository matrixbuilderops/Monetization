"""
However, I will create a script that defines an empty dictionary and asks for user input to add key-value pairs.
Author: Nazmul Idriz
"""
my_dict = {}
    # Ask for user input to add key-value pairs
    key_input = input("Please enter a key (or type 'exit' to quit): ")
        break
    value_input = input(f"Please enter a value for key '{key_input}': ")
    # Add the key-value pair to the dictionary
    my_dict[key_input] = value_input